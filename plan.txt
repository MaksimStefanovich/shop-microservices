1. controllers work with DTO
2. all DTO and entity are validated
3. get all requests work with paginations
4. all selections in DB
6. projections where it is needed
7. unit tests on all services
8. postman collection for every each controller
9. java doc for public methods required, for other methods optional, for controller methods don't needed
10.swagger for controller methods
11.logging all exceptions(ERROR, WARN), controller request response(INFO), controller response request data(DEBUG)
